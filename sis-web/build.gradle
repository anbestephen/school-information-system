apply plugin: 'java'
apply plugin: 'war'
apply plugin: 'jetty'

dependencies {
	compile project(':sis-model')
	compile project(':sis-service')
	providedCompile 'javax.servlet:jstl:1.2'
	compile 'org.springframework:spring-webmvc:4.1.6.RELEASE'
}

httpPort = 8080
stopPort = 9080
stopKey = "stopKey"